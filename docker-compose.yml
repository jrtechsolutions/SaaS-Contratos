# Orquestração da API — variáveis em ./backend/.env (crie a partir do exemplo local)
services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: saas-contratos-api:latest
    container_name: saas-contratos-api
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://127.0.0.1:'+(process.env.PORT||3001)+'/health',(r)=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: always
